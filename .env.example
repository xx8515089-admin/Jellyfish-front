# Java 后端地址。前端 generated client 会从这里读取接口 base URL。
# VITE_BACKEND_URL=http://localhost:8086/jellyfish
# 生产打包默认配置见 .env.production。

# OpenAPI 文档地址。默认会使用 ${VITE_BACKEND_URL}/v3/api-docs。
# OPENAPI_URL=http://localhost:8086/jellyfish/v3/api-docs
