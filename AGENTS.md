# Development Workflow

- Do not automatically start frontend, backend, development, preview, or other long-running services in this workspace.
- Only start a service when the user explicitly requests it. The user manages project services and ports manually.
