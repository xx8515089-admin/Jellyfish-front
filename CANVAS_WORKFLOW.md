# 自由画布功能说明（中文）

更新时间：2026-08-19  
适用范围：`front` 前端项目（自由画布 / Tapnow 嵌套）  
文档角色：可直接给产品、测试、开发同步

---

## 【概览】这份文档在解决什么问题

**一句话目标**  
自由画布是一个“可视化生产链路”：把输入、生成、预览、导出放在同一画布里，支持图像与视频并行产出。

**关键边界**  
- 画布本体为**本地优先**，数据由 `localStorage / indexedDB` 托管（按 workspace 隔离）。  
- 画布核心编辑能力来自 `public/canvas-studio/index.html`（Tapnow）。  
- 外壳页面与路由、列表、持久化入口在 Jellyfish 原有前端代码中实现。

---

## 【标注规则】看文档先看这段

每条功能按统一标签写成 4 列：
- **[角色]**：谁在用（PM/开发/测试/用户）
- **[动作]**：在画布里做什么
- **[输入]**：需要什么前置材料
- **[输出]**：会产生什么结果
- **[判定]**：如何确认是否成功

同时给出 **优先级**：
- **P0**：必须可用
- **P1**：核心体验必须稳定
- **P2**：体验优化/效率提升

---

## 【一、入口与页面流转】

### 1.1 列表入口（Jellyfish 外壳）
- **[角色：PM/用户]**  
  - [动作] 进入 `/projects`  
  - [输入] 查询参数 `view`  
  - [输出] 在“项目”和“画布”之间切换  
  - [判定] 看到两个 Tab（workflow / canvas）

- **[角色：开发]**  
  - [动作] 在 `ProjectLobby.tsx` 管理 `workspaceView`  
  - [输入] 路由 `searchParams.get('view')`  
  - [输出] `workflow` 或 `canvas` 视图状态  
  - [判定] 非 `canvas` 时请求后端项目；`canvas` 时只渲染本地工作区列表

### 1.2 画布创建与打开
- **[角色：用户]**  
  - [动作] 点击“新建画布”  
  - [输入] 画布名称  
  - [输出] 新画布进入列表  
  - [判定] 列表刷新后出现新项

- **[角色：开发]**  
  - [动作] 导向 `/canvas/{canvasId}`  
  - [输入] `canvasId`  
  - [输出] 加载 `CanvasStudioPage.tsx`  
  - [判定] `iframe` 成功挂载，`ready` 事件触发

### 1.3 内核加载（iframe）
- **[角色：用户]**  
  - [动作] 打开画布页面  
  - [输入] `canvasId`  
  - [输出] Tapnow 画布界面  
  - [判定] 能看到节点编辑区域，且页面出现“Opening canvas...” 后正常消失

- **[角色：开发]**  
  - [动作] `CanvasStudioPage` 监听 `postMessage`  
  - [输入] `ready / changed`  
  - [输出] 列表更新时间刷新  
  - [判定] `changed` 到达后 `touchCanvasWorkspace` 执行成功

---

## 【二、数据与隔离机制】

### 2.1 本地画布元数据
- **[角色：开发]**  
  - [动作] 读写画布列表  
  - [输入] `canvasWorkspaces.ts`  
  - [输出] `CanvasWorkspace[]`（`id,name,createdAt,updatedAt`）  
  - [判定] 列表可持久展示、可排序、可更新

### 2.2 画布内数据隔离（核心）
- **[角色：开发]**  
  - [动作] 桥接 `storage` 与 `indexedDB` 命名空间  
  - [输入] `workspaceId`  
  - [输出] key 前缀 `jellyfish_canvas:{workspaceId}:`  
  - [判定] 不同画布互不污染、可独立恢复

- **[角色：开发/测试]**  
  - [动作] 删除画布  
  - [输入] `workspaceId`  
  - [输出] registry 中移除 + 清理 scoped key/数据库  
  - [判定] 回到列表后无法读到该画布历史

---

## 【三、功能按层级标注（可视化对照表）】

## 输入层（P0）
| 标签 | 节点 | 作用 | 关键输入 | 产生结果 | 判定 |
|---|---|---|---|---|---|
| [I1] | `input-image` | 导入图片素材 | 图片文件/URL | 创建图片输入节点，后续可引用 | 画布可继续连接至生成节点 |
| [I2] | `text-node` | 输入提示词/描述 | 文本 | 作为生成上下文 | 可被下游节点读取 |
| [I3] | `novel-input` | 导入小说/长文本 | 长文本文档 | 形成文本素材 | 可用于分镜化/提取流程 |
| [I4] | `video-input` | 导入视频素材 | 视频文件/素材URL | 视频输入节点 | 可用于视频相关节点 |

## 解析层（P0~P1）
| 标签 | 节点 | 作用 | 关键输入 | 产生结果 | 判定 |
|---|---|---|---|---|---|
| [A1] | `extract-characters-scenes` | 提取角色与场景 | 文本内容 | 角色/场景描述候选 | 产出可编辑的角色/场景条目 |
| [A2] | `character-description` | 维护角色文案 | 角色关键词 | 角色设定文本 | 用于稳定角色生成 |
| [A3] | `scene-description` | 维护场景文案 | 场景关键词 | 场景设定文本 | 用于稳定场景生成 |

## 生成层（P0）
| 标签 | 节点 | 作用 | 关键输入 | 产生结果 | 判定 |
|---|---|---|---|---|---|
| [G1] | `generate-character-image` | 生成人物图 | 角色描述 | 角色图片 | 可视化生成结果可见 |
| [G2] | `generate-scene-image` | 生成场景图 | 场景描述 | 场景图片 | 可视化生成结果可见 |
| [G3] | `generate-character-video` | 生成角色视频 | 角色素材/描述 | 视频片段 | 可在预览下载 |
| [G4] | `generate-scene-video` | 生成场景视频 | 场景素材/描述 | 视频片段 | 可在预览下载 |
| [G5] | `gen-image` | 通用图片生成 | 文本/参考图 | 图片 | 可复用与其他图节点 |
| [G6] | `gen-video` | 通用视频生成 | 文本/参考图 | 视频 | 可复用与其他视频节点 |

## 组织与控制层（P0~P1）
| 标签 | 节点 | 作用 | 关键输入 | 产生结果 | 判定 |
|---|---|---|---|---|---|
| [O1] | `storyboard-node` | 分镜编排中心 | 文本、模型参数、shot 数 | shot 列表 + 参数配置 | 可直接触发图/视频批处理 |
| [O2] | `create-character` | 创建角色对象 | 名称/描述 | 角色资产 | 出现在资产体系 |
| [O3] | `create-scene` | 创建场景对象 | 名称/描述 | 场景资产 | 可复用于多 shot |
| [O4] | `local-save` | 画布内保存锚点 | 当前工作区状态 | 本地可恢复草稿 | 刷新后能恢复 |
| [O5] | `preview` | 输出预览入口 | 生成结果 | 预览页签 | 能确认生成效果 |
| [O6] | `image-compare` | 结果对比 | 多张同类输出 | 对比视图 | 易于选择最优版本 |

---

## 【四、常见用户场景（每个场景有明确收敛口径）】

### 场景 A：剧本先行（文案型）
- **[角色]** 用户  
- **[路径]** `novel-input` -> `storyboard-node` -> `gen-image / gen-video`  
- **[目标]** 一个文本出多镜头版本  
- **[验收]** 每个 shot 有独立参数，支持批量执行

### 场景 B：角色资产化
- **[角色]** 用户  
- **[路径]** `extract-characters-scenes` -> `character-description` -> `create-character` -> `generate-character-image`  
- **[目标]** 保持角色一致性  
- **[验收]** 角色风格可在多个镜头复用

### 场景 C：场景风格统一
- **[角色]** 用户  
- **[路径]** `input-image/create-scene/generate-scene-image`  
- **[目标]** 快速检验并统一视觉风格  
- **[验收]** 场景图序列风格一致、可对比输出

### 场景 D：长片段批量产出
- **[角色]** 用户  
- **[路径]** 多个 `storyboard-node` shot -> 批量生成  
- **[目标]** 一次性产出多镜头  
- **[验收]** 队列可见、可暂停/重试

---

## 【五、发布前验收清单（测试视图）】
- **[P0]** 画布新建/重命名/删除/恢复流程完整，列表可见一致。  
- **[P0]** 删除画布后，旧 `scoped` 数据不再可读。  
- **[P0]** `storyboard-node` 能设置 shot 参数（比例、时长、模型、分辨率）并生效。  
- **[P1]** 支持至少一张图 + 一段视频的闭环：输入 → 生成 → 预览 → 下载。  
- **[P1]** 批量生成可执行多个 shot，支持任务队列状态可视化。  
- **[P1]** `local-save` + 刷新 + 重新打开后状态恢复。  
- **[P2]** 结果文件命名可追溯，下载行为稳定。  

---

## 【六、对外统一话术（可直接复制发给同事）】
- “自由画布把创作流程收敛到一张图里，输入到输出都在同一页完成。”  
- “支持文本、图片、视频三类输入，支持角色/场景提取和分镜化生成。”  
- “每个画布数据独立隔离，切换项目不会互相污染。”  
- “先小范围试跑风格，再批量下发，节省重试成本。”

---

## 【七、后续（可选）协同升级点】
- 若要支持多人协作：把当前 `local` registry 升级为服务端元数据，画布内容同步走后端接口。  
- 若要可追溯审计：为关键操作增加 `operationId` 与版本记录。  
- 若要更强恢复能力：画布打开时校验 `workspace` key 完整性并提示迁移状态。

